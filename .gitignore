/dist/
/bin/

# Operator secrets & generated material — MUST never be committed.
/deploy/ca/
/deploy/.env
/deploy/tunneld.env
/deploy/logs/
/deploy/acme/
/deploy/banfiles/
/deploy/attest/
/deploy/ntfy-alertmanager/config.scfg

# attest-probe Gradle build outputs & machine-specific SDK pointer (the built APK fixture under
# fixtures/attest-probe/ IS committed — only these are ignored).
/support/attest-probe/.gradle/
/support/attest-probe/build/
/support/attest-probe/app/build/
/support/attest-probe/local.properties
/support/attest-probe/.idea/
